Card.prototype.create = function(){
    var cardText = document.createTextNode(this.firstName);
    cardArea.appendChild(cardText);
};

for (var i=0; i<devs.length; i++){
    var newCard = new Card(devs[i].firstName,devs[i].lastName,devs[i].role,devs[i].email,devs[i].phone);
    newCard.create();
};


//for(var i=0; i < devs.length; i++){
  //  var newCard = document.createElement('div');
    //cardDisplayArea.appendChild(newCard);
    //for(field in devs[i]){
      //  var newRow = document.createElement('div');
        //newCard.appendChild(newRow);
        //var cardValue = document.createTextNode(devs[i][field]);
        //newRow.appendChild(cardValue);
    //};
//};

